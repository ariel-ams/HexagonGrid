# Pre-Lamp-Style Glow Pollen Backup

This immutable batch preserves `assets/glowPollen.png` from before the Glow Pollen Lamp-style migration on 2026-08-14.

Restore from the repository root with:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File assets/sprite-backups/pre-lamp-style-glow-pollen-2026-08-14/restore.ps1
```

Run `npm test` after restoring.
